import { Link, useLocation } from '@tanstack/react-router'
import { FileText, PenLine, Settings } from 'lucide-react'
import { ReactNode } from 'react'
import { cn } from '@/lib/utils'

const links = [{ to: '/studio', label: '创作', icon: PenLine }, { to: '/works', label: '作品', icon: FileText }, { to: '/settings', label: '设置', icon: Settings }]
export function AppShell({ children }: { children: ReactNode }) { const pathname = useLocation({ select: location => location.pathname }); return <div className="min-h-[100dvh] bg-background lg:grid lg:grid-cols-[216px_minmax(0,1fr)]"><aside className="border-b border-border bg-card lg:min-h-[100dvh] lg:border-r lg:border-b-0"><div className="flex h-16 items-center gap-2 px-5"><span className="grid size-8 place-items-center rounded-md bg-primary text-sm font-semibold text-primary-foreground">笺</span><span className="font-semibold">红笺</span></div><nav className="flex gap-1 overflow-x-auto px-3 pb-3 lg:grid lg:gap-1">{links.map(({ to, label, icon: Icon }) => <Link key={to} to={to} className={cn('flex h-11 shrink-0 items-center gap-2 rounded-md px-3 text-sm text-muted-foreground transition-[background-color,color] hover:bg-muted hover:text-foreground active:scale-[0.98]', pathname === to || (to === '/studio' && pathname.startsWith('/studio/')) ? 'bg-accent font-medium text-accent-foreground' : '')}><Icon size={17} strokeWidth={1.8} />{label}</Link>)}</nav></aside><main className="min-w-0">{children}</main></div> }
